require 'spec_helper'

describe Mina::Yarn do
  it 'has a version number' do
    expect(Mina::Yarn::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
